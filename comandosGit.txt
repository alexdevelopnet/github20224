gitinit  --inicia o git a monitorar a pasta

--configuracao inicial git--------------------
git config --global user.email "alexfnet@gmail.com"
git config --global user.name "alexdevelopnet"

----------------------------------------------------
git add *
git commit -m "inicio"

enviando  o diretorio local para o remoto
git remote add origin https://github.com/alexdevelopnet/github20224.git

empurrando as alterações para o github

git push origin master